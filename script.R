# needs('dplyr')
# data <- read.csv("mergedbudget_rev.csv", na.strings = "")

# attach(input[[1]])

# paste(genre)
# ex-sync.R
# needs(magrittr)
# set.seed(512)
# do.call(rep, input) %>% 
#   strsplit(NULL) %>% 
#   sapply(sample) %>% 
#   apply(2, paste, collapse = "")
print("Hello")